{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.shibbirweb/mcp-db-read-only",
  "title": "Databases (read-only, switchable)",
  "description": "Read-only SQL, MongoDB, Redis and Elasticsearch access, switchable mid-conversation.",
  "version": "0.1.0",
  "websiteUrl": "https://github.com/shibbirweb/mcp-db-read-only#readme",
  "repository": {
    "url": "https://github.com/shibbirweb/mcp-db-read-only",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@shibbirweb/mcp-db-read-only",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "DB_URL",
          "description": "Connection URL to open at startup, e.g. postgres://user@host:5432/db, mongodb://user@host/db, redis://host:6379/0 or sqlite:///data/app.db",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "DB_PASSWORD",
          "description": "Password for DB_URL, so it need not be percent-encoded inside the URL",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "DB_PROFILES",
          "description": "JSON object of named connections to switch between, each a URL or {\"url\", \"password\"}",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "DB_DEFAULT_PROFILE",
          "description": "Which DB_PROFILES entry to start on",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "DB_CONNECT_TIMEOUT_MS",
          "description": "Connection timeout in milliseconds. Defaults to 10000",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "DB_QUERY_TIMEOUT_MS",
          "description": "Server-side query timeout in milliseconds. Defaults to 30000",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "DB_LOG",
          "description": "true logs every tool call, its statements and full output to stderr. Off by default",
          "isRequired": false,
          "format": "boolean"
        },
        {
          "name": "DB_LOG_FILE",
          "description": "Log every tool call to this file instead. Implies DB_LOG",
          "isRequired": false,
          "format": "filepath"
        },
        {
          "name": "DB_LOG_DIR",
          "description": "Save every tool call as its own JSON file in this folder, permanently. Implies logging",
          "isRequired": false,
          "format": "filepath"
        },
        {
          "name": "DB_LOG_FORMAT",
          "description": "pretty (default) or json, one object per line",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "DB_LOG_PORT",
          "description": "Serve a live log viewer in the browser on this port. Needs DB_LOG_DIR, DB_LOG or DB_LOG_FILE. No access control",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "DB_LOG_HISTORY",
          "description": "Entries the viewer keeps in memory when DB_LOG_DIR is not set. Defaults to 500",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "MYSQL_HOST",
          "description": "Legacy MySQL-only form, kept for drop-in compatibility. Prefer DB_URL",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "MYSQL_USER",
          "description": "Legacy MySQL-only form. Prefer DB_URL",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "MYSQL_PASSWORD",
          "description": "Legacy MySQL-only form. Prefer DB_URL with DB_PASSWORD",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "MYSQL_DATABASE",
          "description": "Legacy MySQL-only form. Prefer DB_URL",
          "isRequired": false,
          "format": "string"
        }
      ]
    },
    {
      "registryType": "oci",
      "identifier": "docker.io/shibbirweb/mcp-db-read-only:0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "DB_URL",
          "description": "Connection URL to open at startup, e.g. postgres://user@host:5432/db, mongodb://user@host/db, redis://host:6379/0 or sqlite:///data/app.db. Use host.docker.internal to reach the host",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "DB_PASSWORD",
          "description": "Password for DB_URL, so it need not be percent-encoded inside the URL",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "DB_PROFILES",
          "description": "JSON object of named connections to switch between, each a URL or {\"url\", \"password\"}",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "DB_DEFAULT_PROFILE",
          "description": "Which DB_PROFILES entry to start on",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "DB_CONNECT_TIMEOUT_MS",
          "description": "Connection timeout in milliseconds. Defaults to 10000",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "DB_QUERY_TIMEOUT_MS",
          "description": "Server-side query timeout in milliseconds. Defaults to 30000",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "DB_LOG",
          "description": "true logs every tool call, its statements and full output to stderr. Off by default",
          "isRequired": false,
          "format": "boolean"
        },
        {
          "name": "DB_LOG_FILE",
          "description": "Log every tool call to this file instead, on a mounted volume. Implies DB_LOG",
          "isRequired": false,
          "format": "filepath"
        },
        {
          "name": "DB_LOG_DIR",
          "description": "Save every tool call as its own JSON file in this folder, on a mounted volume. Implies logging",
          "isRequired": false,
          "format": "filepath"
        },
        {
          "name": "DB_LOG_FORMAT",
          "description": "pretty (default) or json, one object per line",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "DB_LOG_PORT",
          "description": "Serve a live log viewer on this port (publish it with -p). Needs DB_LOG_DIR, DB_LOG or DB_LOG_FILE. No access control",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "DB_LOG_HISTORY",
          "description": "Entries the viewer keeps in memory when DB_LOG_DIR is not set. Defaults to 500",
          "isRequired": false,
          "format": "number"
        },
        {
          "name": "MYSQL_HOST",
          "description": "Legacy MySQL-only form, kept for drop-in compatibility. Prefer DB_URL",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "MYSQL_USER",
          "description": "Legacy MySQL-only form. Prefer DB_URL",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "MYSQL_PASSWORD",
          "description": "Legacy MySQL-only form. Prefer DB_URL with DB_PASSWORD",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "MYSQL_DATABASE",
          "description": "Legacy MySQL-only form. Prefer DB_URL",
          "isRequired": false,
          "format": "string"
        }
      ]
    }
  ]
}
